version: '3.8'

services:
  app:
    build: .
    container_name: ai_trading_dashboard
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///data/trading_dashboard.db
      - ENVIRONMENT=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
